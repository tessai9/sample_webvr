// eslint-disable-next-line no-undef
AFRAME.registerComponent("player", {
  schema: {
    foo: {type: "string"}
  },

  init: function () {
    // console.log("player initialization")
    // var el = this.el;
    // var data = this.data;
  },

  update: function () {
    // var el = this.el;
    // var data = this.data;
  }
});
